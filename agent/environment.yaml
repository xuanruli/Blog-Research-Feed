name: blog-research-feed-env-v13
config:
  type: cloud

  networking:
    type: unrestricted

  packages:
    apt:
      - ffmpeg
      - jq
    pip:
      - "setuptools<72"
      - "wheel"
      - "blog-research-feed==0.2.9"
